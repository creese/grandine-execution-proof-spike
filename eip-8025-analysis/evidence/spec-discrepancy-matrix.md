# EIP-8025 Specification Discrepancy Matrix

Compared sources: EIP `3aa6b30641ef8f98f8945b29ee92c1e8aed1070a`; consensus-specs `8c12caee279d77b322446d33440b37479117dcde`. The consensus feature files explicitly call themselves work in progress, and `p2p-interface.md` says execution values are not definitive.

| Topic | EIP | Consensus specs | Difference | Significance | Open question |
|---|---|---|---|---|---|
| Proof byte bound | 409,600 bytes / 400 KiB (`eip-8025.md:131-145`) | 4,194,304 bytes / 4 MiB (`beacon-chain.md:41-48`) | 10.24× | SSZ limit/root, codec, gossip bandwidth and DoS interoperability | Which bound is canonical? |
| Proof byte SSZ type | `ByteList[MAX_PROOF_SIZE]` (`171-174`) | `ProgressiveByteList` plus explicit max check (`beacon-chain.md:64-71,90-108`) | Different serialization model | Encodings and hash-tree roots may be incompatible | Is progressive SSZ required, and how does max apply? |
| Signature domain | `0x0D000000` (`131-136`) | `0x0F000000` (`beacon-chain.md:49-54`) | Direct contradiction | Every cross-source signature fails | Which domain is assigned? |
| Public input | Root + successful-validation boolean + chain config (`177-199`) | Root only (`beacon-chain.md:57-62`) | Spec omits two EIP fields | Different SSZ roots; success/config cannot be independently checked | Was the public input deliberately simplified or is the feature stale? |
| Verification success/config | Expected guest, true result, matching root/config (`476-483`) | Cryptographic verification is implementation-dependent; container cannot carry result/config (`proof-engine.md:45-56`) | EIP-only validation | Cross-chain/fork replay and negative-result handling are underspecified in CL feature | Where are guest identity, success and chain config cryptographically bound? |
| Quorum `k` | Payload proof-verified after `k`; value promised before Review (`252-260`) | “Sufficient execution proofs” with no threshold (`p2p-interface.md:225-235`) | Unresolved placeholder | Status, sync target, cache state and operator behavior cannot interoperate reliably | Is quorum global, per-node, per proof type, or exactly one? |
| Proof processing location | Explicitly outside beacon-block transition and no on-chain change (`346-354`) | Presented beneath “Beacon chain state transition function” (`beacon-chain.md:82-108`) | Structural ambiguity | Implementers could mistakenly put optional proof failure on consensus transition path | Should feature docs explicitly state operational/out-of-transition invocation? |
| Payload notification context | EIP example modifies `process_block/process_execution_payload` (`275-319`) | Gloas fork-choice verifies `SignedExecutionPayloadEnvelope`, then notifies (`fork-choice.md:24-110`) | Different base architecture | Call timing/data source differs | Is the EIP prose stale relative to its EIP-7732/Gloas dependency? |
| Prover event/data | `Block`/`BeaconBlock` event and fetch (`438-445`) | `execution_payload` event, envelope and committed bid (`prover.md:52-64`) | Terminology and object mismatch | `NewPayloadRequest` construction integration differs | Which event and API object are normative? |
| Gossip first prerequisite | “[IGNORE] root has been seen” while prose permits queueing (`364-366`) | “proof … request … has been seen,” also as IGNORE (`p2p-interface.md:89-92`) | Normative polarity is awkward | Literal reading says known roots are ignored; intended meaning appears “must have been seen” | Rewrite as a positive prerequisite or explicit unknown-root condition? |
| Gossip dedup | Payload/type valid-first and payload/type/prover first attempt (`367-372`) | Adds proof-root dedup; repeats payload/type rule after reject checks (`p2p-interface.md:87-112`) | Extra and duplicated rule | Validation order/cache semantics differ | Is final duplicate bullet intentional post-verification race protection? |
| Empty data check | Included in gossip reject and positive proof semantics (`376-378`) | Gossip rejects empty; `process_execution_proof` only explicitly checks maximum (`p2p-interface.md:106-109`; `beacon-chain.md:96-108`) | Layering difference | Direct non-gossip invocation might accept empty bytes if engine does | Must the processing function independently enforce non-empty? |
| Serving-range notation | `[finalized_checkpoint.slot, current_slot]` (`410-413`) | start slot computed from finalized checkpoint epoch (`p2p-interface.md:146-150`) | EIP uses a checkpoint field that is normally epoch-based | Boundary ambiguity | Should EIP use `compute_start_slot_at_epoch(finalized_checkpoint.epoch)`? |
| Signed proof max | No separate signed-object bound | `MAX_SIGNED_EXECUTION_PROOF_SIZE = 4,194,449` (`p2p-interface.md:45-50`) | Spec-only bound | Codec/rate-limiter sizing needs a definitive value tied to proof encoding | Recompute after proof size/container resolution? |
| Range request representation | Tuple-like description (`393-396`) | Request shown as anonymous tuple and response list/chunks (`p2p-interface.md:118-166`) | Spec elaboration, but container naming absent | Client SSZ encoding can diverge if tuple/container conventions differ | Should a named SSZ container be normative? |
| Dynamic proof types | Socialized values, no hard fork/release (`147-155`) | Dynamic advertised capability (`p2p-interface.md:225-235`) | Broad agreement but no registry/version negotiation | Same `uint8` may acquire conflicting meanings | What allocates values and binds a proof type to guest/verifier identity? |
| Activation/base fork | No EIP hardfork; fully optional (`1134-1136`) | Feature is built upon Gloas (`beacon-chain.md:30-31` and peers) | Runtime optionality depends on a fork-specific payload architecture | Deployability boundary is unclear | Can EIP-8025 run on any fork, or only once Gloas/EIP-7732 is active? |
| Tests | Links pinned consensus feature directory as “Test Cases” (`1138-1141`) | No tests in checked-out feature directory | Missing artifacts | Normative behavior lacks local vector coverage | Where are CL vectors, especially negative gossip and SSZ/root cases? |
| EL source availability | EIP delegates canonical EL semantics to pinned execution-specs revision (`61-72`) | Workspace consensus feature contains no EL spec | Source-set gap | Phase 1 can index EIP statements but cannot independently verify canonical EL implementation | Should execution-specs be added as a read-only source for later proposal work? |

## Lighthouse alignment preview (scope evidence only)

**Verified Lighthouse facts:** branch-local `consensus/types/src/execution/eip8025.rs:19-42,51-70` uses a 1,376,256-byte bounded `VariableList`, four proofs, domain `0x0D`, and root-only `PublicInput`. Thus it follows the EIP on domain and fixed `ByteList` style, follows the checked-out consensus feature on root-only public input, and follows neither source on maximum proof size. This hybrid is a Phase 2 investigation priority, not a resolution.
